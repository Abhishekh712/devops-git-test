name: Hello Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello
        run: echo "Hello Abhishekh! GitHub Actions is working 🎉"
